type Rgba = readonly [number, number, number, number];

/** Matches --color-bg (#05070a). */
export const BACKGROUND: Rgba = [5 / 255, 7 / 255, 10 / 255, 1];

/** Matches --color-alive (#7ff3ff). */
export const ALIVE: Rgba = [127 / 255, 243 / 255, 255 / 255, 1];
